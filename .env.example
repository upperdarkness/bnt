# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blacknova
DB_USER=bnt
DB_PASS=bnt

# Security
# Generate a secure admin password hash using: php -r "echo password_hash('your_password', PASSWORD_DEFAULT);"
# Then update config/config.php with the hash
